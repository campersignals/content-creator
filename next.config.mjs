/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        // serverActions: true, // Default in Next 14
    },
    output: 'standalone',
};

export default nextConfig;
